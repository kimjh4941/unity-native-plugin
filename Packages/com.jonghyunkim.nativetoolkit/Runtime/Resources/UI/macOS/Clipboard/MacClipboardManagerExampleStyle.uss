/* Duplicated from the iOS clipboard sample style rather than shared: common.md keeps
   platform features on their own files, so the two can diverge without affecting each
   other. Class names carry the Mac prefix to match MacClipboardManagerExample.uxml. */
.mac-clipboard-root {
    flex-grow: 1;
    background-color: rgba(0, 0, 0, 1);
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
}

/*
 * Capped at half the screen: on a device the header would otherwise take most of it, leaving the
 * button list a few rows tall. The explanatory text lives in the scrolling About section instead.
 */
.mac-clipboard-header {
    background-color: rgba(255, 255, 255, 1);
    padding: 12px 16px;
    gap: 6px;
    flex-shrink: 0;
    max-height: 50%;
    display: flex;
    flex-direction: column;
}

.mac-clipboard-title {
    font-size: 20px;
    color: #1C1C1E;
    -unity-font-style: bold;
    -unity-text-align: upper-center;
    white-space: normal;
}

/*
 * Fixed height: results are single lines, but a long native error message wraps. Letting the
 * result area grow would push the button list off screen, so the text scrolls inside instead.
 * flex-shrink: 0 keeps the header from compressing it.
 */
.mac-clipboard-result-border {
    background-color: #EEEEEE;
    border-radius: 8px;
    height: 64px;
    flex-shrink: 0;
}

.mac-clipboard-result-scroll {
    flex-grow: 1;
}

.mac-clipboard-result-text {
    padding: 10px;
    font-size: 12px;
    color: #212121;
    -unity-text-align: upper-left;
    white-space: normal;
}

.mac-clipboard-status-text {
    font-size: 12px;
    color: #1C1C1E;
    -unity-font-style: bold;
    -unity-text-align: upper-left;
    white-space: normal;
}

.mac-clipboard-scroll {
    flex-grow: 1;
    min-height: 50%;
}

.mac-clipboard-content {
    background-color: rgba(255, 255, 255, 1);
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 0 16px 16px 16px;
    gap: 12px;
}

.mac-clipboard-section {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.mac-clipboard-section-title {
    font-size: 15px;
    color: #263238;
    -unity-font-style: bold;
    white-space: normal;
}

.mac-clipboard-section-note {
    font-size: 11px;
    color: #757575;
    white-space: normal;
}

.mac-clipboard-button {
    background-color: #007AFF;
    color: white;
    font-size: 14px;
    width: 100%;
    min-height: 46px;
    height: auto;
    -unity-text-align: middle-center;
    border-width: 0;
    border-radius: 8px;
    white-space: normal;
    overflow: visible;
    padding: 8px 12px;
}

.mac-clipboard-button:hover {
    background-color: #0056D6;
}

.mac-clipboard-button:active {
    background-color: #0046AA;
}

.mac-clipboard-button:disabled {
    background-color: #CCCCCC;
    color: #999999;
}

.mac-secondary-button {
    background-color: #8E8E93;
    color: white;
    font-size: 14px;
    width: 100%;
    min-height: 46px;
    height: auto;
    -unity-text-align: middle-center;
    border-width: 0;
    border-radius: 8px;
    white-space: normal;
    overflow: visible;
    padding: 8px 12px;
}

.mac-secondary-button:hover {
    background-color: #6E6E73;
}

.mac-secondary-button:active {
    background-color: #5A5A5E;
}

.mac-secondary-button:disabled {
    background-color: #CCCCCC;
    color: #999999;
}
