/* Duplicated from the macOS clipboard sample style rather than shared: common.md keeps
   platform features on their own files, so the two can diverge without affecting each
   other. Class names carry the Windows prefix to match WindowsClipboardManagerExample.uxml. */
.windows-clipboard-root {
    flex-grow: 1;
    background-color: rgba(0, 0, 0, 1);
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
}

/*
 * Capped at half the screen: on a device the header would otherwise take most of it, leaving the
 * button list a few rows tall. The explanatory text lives in the scrolling About section instead.
 */
.windows-clipboard-header {
    background-color: rgba(255, 255, 255, 1);
    padding: 12px 16px;
    gap: 6px;
    flex-shrink: 0;
    max-height: 50%;
    display: flex;
    flex-direction: column;
}

.windows-clipboard-title {
    font-size: 20px;
    color: #1C1C1E;
    -unity-font-style: bold;
    -unity-text-align: upper-center;
    white-space: normal;
}

/*
 * Fixed height: results are single lines, but a long native error message wraps. Letting the
 * result area grow would push the button list off screen, so the text scrolls inside instead.
 * flex-shrink: 0 keeps the header from compressing it.
 */
.windows-clipboard-result-border {
    background-color: #EEEEEE;
    border-radius: 8px;
    height: 64px;
    flex-shrink: 0;
}

.windows-clipboard-result-scroll {
    flex-grow: 1;
}

.windows-clipboard-result-text {
    padding: 10px;
    font-size: 12px;
    color: #212121;
    -unity-text-align: upper-left;
    white-space: normal;
}

/*
 * The lifecycle line sits above the result area rather than in the status line: Draining and
 * ShutdownFailed both answer with the same error code, so this is the only place the difference
 * shows, and it must not scroll away with the log.
 */
.windows-clipboard-state-text {
    font-size: 12px;
    color: #B71C1C;
    -unity-font-style: bold;
    -unity-text-align: upper-left;
    white-space: normal;
}

.windows-clipboard-status-text {
    font-size: 12px;
    color: #1C1C1E;
    -unity-font-style: bold;
    -unity-text-align: upper-left;
    white-space: normal;
}

.windows-clipboard-scroll {
    flex-grow: 1;
    min-height: 50%;
}

.windows-clipboard-content {
    background-color: rgba(255, 255, 255, 1);
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 0 16px 16px 16px;
    gap: 12px;
}

.windows-clipboard-section {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.windows-clipboard-section-title {
    font-size: 15px;
    color: #263238;
    -unity-font-style: bold;
    white-space: normal;
}

.windows-clipboard-section-note {
    font-size: 11px;
    color: #757575;
    white-space: normal;
}

.windows-clipboard-button {
    background-color: #007AFF;
    color: white;
    font-size: 14px;
    width: 100%;
    min-height: 46px;
    height: auto;
    -unity-text-align: middle-center;
    border-width: 0;
    border-radius: 8px;
    white-space: normal;
    overflow: visible;
    padding: 8px 12px;
}

.windows-clipboard-button:hover {
    background-color: #0056D6;
}

.windows-clipboard-button:active {
    background-color: #0046AA;
}

.windows-clipboard-button:disabled {
    background-color: #CCCCCC;
    color: #999999;
}

.windows-secondary-button {
    background-color: #8E8E93;
    color: white;
    font-size: 14px;
    width: 100%;
    min-height: 46px;
    height: auto;
    -unity-text-align: middle-center;
    border-width: 0;
    border-radius: 8px;
    white-space: normal;
    overflow: visible;
    padding: 8px 12px;
}

.windows-secondary-button:hover {
    background-color: #6E6E73;
}

.windows-secondary-button:active {
    background-color: #5A5A5E;
}

.windows-secondary-button:disabled {
    background-color: #CCCCCC;
    color: #999999;
}

.windows-clipboard-field {
    font-size: 12px;
    color: #212121;
}

.windows-clipboard-field > .unity-base-field__label {
    color: #263238;
    min-width: 120px;
}
