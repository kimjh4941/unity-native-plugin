# 検査2「サンプルシーン整合」で意図的に許容する文字列リテラル・型名・メンバー名。
#
# 1 行 1 エントリ。`#` 以降は行コメント。
# クォート付き（"public.png"）でもクォートなし（public.png）でも一致します。
# メンバーは `型名.メンバー名` の形で書きます（例: IosClipboardManager.Foo）。
#
# ここに足す前に、本当に ExampleController と乖離させてよいかを確認してください。
# 「利用者が自分の値に置き換える前提の説明用の値」だけが対象です。
# 実際に動く操作の ID・UTI・パラメータ値は、必ず ExampleController と一致させます。

# 利用者が自分のファイル名に置き換える前提のパス。share.md の 3 言語が同じ値を使っています。
my_image.png
