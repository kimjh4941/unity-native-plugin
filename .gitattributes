# unity-native-plugin.slnx is auto-regenerated by Unity Editor with CRLF line endings.
# Allow trailing CR so `git diff --check` does not flag it as whitespace error.
unity-native-plugin.slnx whitespace=cr-at-eol
