# 実装結果レポート v6（レビュー 5 ラウンド完了時点）

## 基本情報

- 日付: 2026-09-08
- 機能名: clipboard / 対象プラットフォーム: Windows
- ブランチ: `feature/UNT-11`（`ba042a4` 時点）
- 実装計画: `artifact/designs/clipboard/2026-09-05-windows-clipboard-design-v8.md`
- 前回の結果ファイル: `2026-09-07-...-result-v5.md`（レビュー v2 対応時点。**4 ラウンド前の状態で古い**）

## 0. この文書の位置づけ

**止める基準（A 区分 0）は満たしていない。** 本文書は「完了報告」ではなく、
**基準未達を承知でサンプルシーン工程へ進むための現状確定**である。

なぜそう判断したかは 6 節に記す。

---

## 1. 検証ゲートの現状

| ゲート | 結果 |
|---|---|
| 層 1 EditMode | **749 / 749 passed** |
| 層 2a PlayMode（Editor 内） | **181 / 181 passed** |
| **層 0 Win64 Player ビルド** | **成功**（エラー 0 / 本実装由来の警告 0） |
| 機械照合 `check_design_consistency.py` | FAIL 0 |

一括実行: `scripts/verify_unity_windows.sh`

**層 0 は 2026-09-08 に新設した。** 層 1 と層 2a はどちらも `UNITY_EDITOR` 定義下でしか動かず、
`#if UNITY_STANDALONE_WIN && !UNITY_EDITOR` の内側を**一度もコンパイルしない**。
この穴により、Player ビルドが 3 ラウンドにわたって壊れたまま
「コンパイルエラー 0」と報告され続けていた（レビュー v4 A-1）。

## 2. レビューの経過

| ラウンド | レビュアー | A | 主な内容 |
|---|---|---|---|
| v1 | サブエージェント | 0 | ステップ 1（純粋層）のみ対象 |
| v2 | サブエージェント 6 名 | 15 | Manager 全体を初めて対象化 |
| v3 | Codex `gpt-5.6-sol` medium | 5 | **うち 3 件は v2 の修正が持ち込んだもの** |
| v4 | Codex `gpt-5.6-sol` high | 6 | Player ビルド破壊。drain の状態表現を診断 |
| v5 | Codex `gpt-5.5` high | 4 | **全件が drain 終端**。終端遷移の所有が分散していると診断 |

### 領域別の収束

| 領域 | v2 | v5 |
|---|---|---|
| shutdown / drain | 5 | **3** |
| 非同期・配送 | 5 | **1** |
| 読み出し | 2 | **0** |
| 遅延レンダリング | 1 | **0** |
| ライフサイクル・その他 | 2 | **0** |

**drain 終端の一点に絞り込まれた。** 他の領域は 2 ラウンド連続で 0 件。

## 3. 仕組みとして直したもの

指摘を個別に潰すのではなく、それを生む構造を 3 回変更した。

| # | 変更 | 何を不可能にしたか |
|---|---|---|
| 1 | `RunShutdownAttempt` がネイティブ試行と終端処理の両方を持つ（`dad06ad`） | 試行だけして終端処理を忘れること |
| 2 | drain を 5 個のフラグから 1 個の `DrainSession` へ。コルーチンをやめ `Update` 駆動に（`d627d09`） | runner が観測されずに死ぬこと。配送待ちと配送済みを取り違えること |
| 3 | `SettleDrain` が終端のすべて（状態・配送・quit 再開）を持つ（`ba042a4`） | **終端経路ごとに quit 再開を忘れること** |

3 の時点で `ResumeQuit` の呼び出しは**コード全体で 1 箇所**になった。

## 4. ネイティブ境界の実行状況

`2026-09-08-windows-clipboard-smoke-verification-result-v1.md` を参照。

| 公開 API | ネイティブ実行 |
|---|---|
| `Initialize` / `CopyPlainText` / `PastePlainText` / `GetFormats` / quit ドレイン | **実行済み・成功** |
| 上記以外の **21 種** | **一度も実行されていない** |

未実行の内訳: 履歴 API 5 種（非同期）、遅延レンダリング 2 種、イベント 12 種、
`CopyHtml` / `CopyFiles` / `CopyImage` / `CopyCustomFormat` / `CopyMultipleFormats` /
`Clear` / `PasteHtml` / `PasteFiles` / `PasteImage` / `PasteCustomFormat` /
`HasFormat` / `GetPreferredFormat` / `SetHistoryEventsEnabled` / `CancelRequest`。

**これらはコードレビューでは検証できない。** M-1 〜 M-24 の担当であり、
サンプルアプリが無いと実施できない。

## 5. ミューテーション検証

修正 5 スライスで 31 変異を適用し、すべて検出した。ただし次を記録する。

### 死んだテストを 5 件見つけた（すべてミューテーション検証が発見）

| # | テスト | なぜ落ちなかったか |
|---|---|---|
| 1 | `EveryNativeErrorCode_HasAMessage` | フォールバックがあり網羅チェックが常に通る |
| 2 | `ADeliveredRequestGivesBackItsCancellationRegistration` | 観測対象が測定時点で既に消えていた |
| 3 | `Availability_AKeyNameInsideAValue...` | 入力に判別力が無く、修正前でも弾かれた |
| 4 | `ACallerArrivingWhileTheDrainSettles...` | 意図と別の経路で通っており、対象の分岐を踏んでいなかった |
| 5 | `ARequestRejectedDuringAQuitSettle...` | Editor は疑似 quit 後もフレームが回るため、同期と非同期の差が見えなかった |

**いずれも「通ることの確認」では検出できない。** 5 件とも私が書いたテストである。

### 変異は互いを隠す

同じ経路に触れる変異をまとめて適用すると相殺が起きた（I1↔I6、K2↔K4）。
いずれも単独で回して初めて検出された。**バッチで全滅しても、それは各変異が検出された証拠にならない。**

### v4 の記録の訂正（再掲）

「11 変異すべて検出＝テストが有効」は、**変異を注入した箇所についてのみ正しい**。
自分で選んだ箇所に自分で変異を入れる限り、変異の選定が自分の盲点を継承する。

## 6. 基準未達のまま次工程へ進む判断

### 満たしていないもの

止める基準は「A が 0」。**v5 の A 4 件を修正したが、修正後に A が 0 であることは誰も確認していない。**
6 ラウンド目は回さないと事前に決めたため、この状態で先へ進む。

### そう判断した理由

1. **公開 API 26 種のうち 21 種が一度も実行されていない。** managed のロジックを 5 ラウンド磨く間、
   API 表面の 8 割は動いていない。6 ラウンド目より、未実行の 21 種を動かす方が価値が高い
2. **領域が収束している。** 読み出し・遅延レンダリング・レジストリ・結果型・JSON は 2 ラウンド連続 0 件
3. **残った一点は構造で塞いだ。** 規律ではなく不変条件（全経路が `SettleDrain` を通る）

### 残件（手動確認では拾えないもの）

**サンプルアプリを触っても踏まない経路であり、「バグ対応で拾える」と考えてはならない。**

| # | 内容 | 状態 |
|---|---|---|
| R-1 | v5 の A 4 件の修正が、また新しい欠陥を持ち込んでいないか | **未検証**（レビュー未実施） |
| R-2 | 履歴 API・遅延レンダリング・イベントのネイティブ経路 | 未実行。M-1 〜 M-24 の担当 |
| R-3 | IL2CPP での `MonoPInvokeCallback` の実挙動 | 未確認（V-7）。本検証はすべて Mono |
| R-4 | 成功した shutdown がログに何も残さない | 観測性の問題。実機記録が「不在による証明」になる |
| R-5 | イベント購読者の隔離が Windows のみ先行 | `artifact/EVENT_SUBSCRIBER_ISOLATION.md`。他 3 プラットフォームは別タスク |
| R-6 | `s_renderStaging` の再入窓を作る seam が無い | レビュー v5 B-1。設計 9.2 が要求する `InvokeRenderDuringReserveForTests` 未実装 |
| R-7 | Android / iOS / macOS の層 0（Player ビルドゲート） | 未整備。Windows と同じ穴が空いている可能性が高い |

## 7. 変更ファイル（`284a607..HEAD`）

Runtime 5 / テスト 6 / スクリプト 1（新規 `scripts/verify_unity_windows.sh`）/
ルール 1（`agent-rules/coding-rules/testing.md` に層 0 を追加）。
**新規 Runtime ファイルなし。他プラットフォームのファイル変更 0 件。**

## 8. 次

1. `design-sample-scene` → `implement-sample-scene`
   **M-1 〜 M-24 との対応を設計に含めること。** デモではなく検証用として作らないと、
   作ったのに確認できない項目が残る
2. 実機確認 M-1 〜 M-24。**初回は落ちる前提**（未実行の 21 種を初めて動かすため）
3. R-1（再レビュー）は、サンプル実装後に差分をまとめて 1 回で行う方が効率が良い
